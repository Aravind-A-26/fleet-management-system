# Optional PostgreSQL configuration. Local startup defaults to file-backed H2 SQL.
DB_URL=jdbc:postgresql://localhost:5432/fleet
DB_USER=fleet
DB_PASSWORD=replace_with_a_local_password
POSTGRES_DB=fleet
POSTGRES_USER=fleet
POSTGRES_PASSWORD=replace_with_a_local_password
PORT=8080
SERVER_ADDRESS=127.0.0.1
